set -g default-terminal tmux-256color
set -g status off
# Exit only this private server, without Hollywood's broad upstream pkill trap.
bind-key -n C-c kill-server
# Stop the widgets when the last Hollywood terminal closes or detaches.
set -g destroy-unattached on
