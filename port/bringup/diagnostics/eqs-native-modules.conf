# Include AFTER the parent /vendor_dlkm mount: bind of that parent is not recursive.
# Stock eqs /vendor/lib/modules points to /vendor_dlkm/lib/modules.
# Required (not optional); this flat tree belongs to the running diagnostic kernel.
lxc.mount.entry = /run/eqs-modules vendor_dlkm/lib/modules none bind,ro 0 0
